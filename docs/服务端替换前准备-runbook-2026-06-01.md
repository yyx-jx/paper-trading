# 服务端替换前准备 Runbook（2026-06-01）

本文记录 2026-06-01 这次服务端替换前的准备结果，以及真正切换时应执行的最小步骤。

适用范围：

- 只替换后端服务目录和容器镜像
- 不替换 Windows 客户端
- 不做数据库业务数据备份
- 不在本次准备阶段停服务或切换 `/srv/p-t/app`

当前基线：

- 本地版本：`0.6.3`
- 预置 release：`P_T_0.6.3_20260601_084327`
- 远程 staged 目录：`/srv/p-t/releases/P_T_0.6.3_20260601_084327/server`
- 当前生产入口：`http://103.147.13.98:10001`
- 当前 schema 目标：`EXPECTED_SCHEMA_MIGRATION_ID=000008`

## 1. 已完成的准备

本地已通过：

```bash
npm run typecheck
npm run build
npm run test:deployment
npm run test:deployment-readiness
npm run test:migration-safety
npm run test:order-transactions
npm run test:redeem-idempotency
npm run test:trading
```

本地已生成 release：

```text
release/P_T_0.6.3_20260601_084327/
  server/
  checksums.sha256
```

本地已确认：

- release 包内不含 `.env`
- release 包内不含 `data/`
- release 包内不含 `backups/`
- release 包内不含 `node_modules/`
- release 包内不含 `dist/`
- `server/db/migrations/000008_manual_settlement_queue_indexes.sql` 存在
- `server/.env.production.example` 仍是 `EXPECTED_SCHEMA_MIGRATION_ID=000008`

远程已完成：

- 上传到 `/srv/p-t/releases/P_T_0.6.3_20260601_084327/`
- `sha256sum -c checksums.sha256` 通过
- staged release 的 `docker compose config` 通过
- staged release 的 `docker compose build app-server matching-service` 通过
- 构建日志：`/tmp/p_t_build_20260601.log`
- 构建退出码：`/tmp/p_t_build_20260601.exit`，结果为 `0`
- 构建后线上 `live`/`ready` 仍为 `ok:true`
- 当前线上 `schemaMigration` 仍为 `000008`

## 2. 真正切换时执行

先保存代码回滚快照：

```bash
ssh root@103.147.13.98 "bash -lc 'ts=$(date +%Y%m%d_%H%M%S); mkdir -p /srv/p-t/rollback/$ts; cp -a /srv/p-t/app /srv/p-t/rollback/$ts/app; echo $ts'"
```

停止业务容器，但不停止 `postgres` 和 `redis`：

```bash
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -f docker-compose.deploy.yml --env-file .env.production stop caddy app-server matching-service'"
```

替换服务目录，保留现有生产环境文件：

```bash
ssh root@103.147.13.98 "bash -lc 'cp -a /srv/p-t/app/.env.production /tmp/p-t.env.production && rm -rf /srv/p-t/app && mkdir -p /srv/p-t/app && cp -a /srv/p-t/releases/P_T_0.6.3_20260601_084327/server/. /srv/p-t/app/ && cp -a /tmp/p-t.env.production /srv/p-t/app/.env.production'"
```

执行迁移并确认状态：

```bash
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -p app -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npm run db:migrate'"
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -p app -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npm run db:status'"
```

使用已预构建镜像重建容器：

```bash
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -p app -f docker-compose.deploy.yml --env-file .env.production up -d --no-build --force-recreate caddy app-server matching-service'"
```

## 3. 切换后验收

外部健康检查：

```bash
curl.exe -fsS http://103.147.13.98:10001/api/health/live
curl.exe -fsS http://103.147.13.98:10001/api/health/ready
```

远程容器与日志检查：

```bash
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -p app -f docker-compose.deploy.yml --env-file .env.production ps'"
ssh root@103.147.13.98 "bash -lc 'cd /srv/p-t/app && APP_ENV_FILE=.env.production docker compose -p app -f docker-compose.deploy.yml --env-file .env.production logs --tail=200 app-server matching-service caddy'"
```

通过标准：

- `ready` 返回 `ok:true`
- `schemaMigration` 为 `000008`
- `app-server`、`matching-service`、`caddy` 正常运行
- 日志中没有 migration missing、schema mismatch、Unhandled、Write blocked 等关键错误

## 4. 注意事项

- 本次以 `000008` 为唯一有效 schema 口径，旧文档里的 `000005` 说明全部视为过期。
- 不执行数据库业务数据备份；本次只保留代码回滚快照。
- 禁止执行 `docker compose down -v`。
- 切换阶段必须使用 `--no-build --force-recreate`，不要在真正切换时重新走远程构建。
