# 重构验证记录

## 基线

基底：

```text
branch: codex/deploy-backend-layering
base: deploy@d0f9934
```

基线测试已通过：

```bash
npm run typecheck
npm run test:regression
npm run test:trading
npm run test:order-transactions
npm run test:electron-config
npm run build
```

## 本轮改动

- 抽出 `MarketPayloadBuilder`
- 抽出 `UserPayloadBuilder`
- 抽出 `MarketWsSession`
- 抽出 `UserWsSession`
- 抽出 health/metrics route group
- 抽出 auth/me/bootstrap route group
- 抽出 WS route group
- 更新回归脚本，使其检查新的模块边界
- 新增接口与分层文档

## 追加阶段：2026-05-13 23:00 +08:00

本阶段继续在隔离 worktree `D:\P_T-worktrees\deploy-backend-layering` 中执行，不修改原 `D:\P_T` 工作区。

改动内容：

- 新增 `apps/server/src/routes/auth-me.ts`。
- 将 `/api/auth/login`、`/api/ws/tickets`、`/api/me`、`/api/bootstrap/full`、`/api/me/language`、`/api/me`、`/api/me/password` 从 `apps/server/src/index.ts` 抽出。
- `bootstrap/full` 继续使用 `MarketPayloadBuilder` 和 `UserPayloadBuilder`，保持响应结构不变。
- 更新 `scripts/flow-regression-check.ts`，新增 route layering 断言，防止 auth/me/bootstrap 路由回流到 `index.ts`。
- 更新接口总览和分层结构说明。

验证结果：

```bash
npm run typecheck              # passed
npm run test:regression         # passed
npm run test:trading            # passed
npm run test:order-transactions # passed
npm run test:electron-config    # passed
npm run build                   # passed
```

下一步建议：

- 继续抽出 `users` route group，范围包含单用户管理、批量创建、CSV 模板下载与预览。
- 每抽一组继续保持“只移动注册位置和依赖注入，不改业务行为”的策略。

## 最终验收

重构后已通过：

```bash
npm run typecheck              # passed
npm run test:regression         # passed
npm run test:trading            # passed
npm run test:order-transactions # passed
npm run test:electron-config    # passed
npm run build                   # passed
```

## 风险与边界

- 本轮不改数据库 schema。
- 本轮不改前端协议。
- 本轮不迁移 `market:fast_tick` / `market:book`。
- 本轮不修改生产环境文件和真实部署配置。
- 文档不包含公网 IP、真实账号、密码、token。
