# 强化版上线计划书

## 任务表

```text
[x] Phase 0 远程预检
[x] Phase 1 上线阻断项修复
[x] Phase 2 本地测试与打包
[x] Phase 3 上传与部署
[x] Phase 4 数据库迁移与 Admin 创建
[x] Phase 5 外部访问与客户端 smoke
[x] Phase 6 文档和回滚确认
[x] Phase 7 客户端连接压力测试与稳定性修复
[x] Phase 8 镜像备份与构建通道确认
[x] Phase 9 Windows 客户端代理兼容修复
```

## Phase 0 远程预检

完成时间：2026-05-08 20:11  
执行命令：

```bash
ssh -p 22 root@103.147.13.98
docker --version
docker compose version
df -h /
free -h
ss -lntp | grep 10001 || true
```

结果：22/tcp 可连通；新 root 密码 SSH 认证成功；Docker 与 Docker Compose 可用；10001 部署前不可连通，原因是无监听服务。  
遗留风险：根分区可用空间约 15G，低于原 20G 门槛；已执行 Docker 清理并完成部署。  
下一步：已进入 Phase 3。

## Phase 1 上线阻断项修复

完成时间：2026-05-08 20:07  
执行命令：本地代码修改与测试。  
结果：已补齐 HTTP 10001、临时 HTTP 客户端打包开关、生产不 seed 默认账号、create-admin、release、文档和迁移保护。  
遗留风险：HTTP 是临时明文方案；后续正式版仍需 HTTPS。  
下一步：远程凭据可用后上传部署。

## Phase 2 本地测试与打包

完成时间：2026-05-08 20:07  
执行命令：

```bash
npm ci
npm run typecheck
npm run build
npm run test:config
npm run test:permissions
npm run test:deployment
npm run test:deployment-readiness
npm run test:migration-safety
npm run test:order-transactions
npm run test:redeem-idempotency
npm run test:electron-config
npm run package:release
ALLOW_INSECURE_PROD_HTTP=true VITE_API_BASE_URL=http://103.147.13.98:10001 npm run package:win:prod
```

结果：本地硬门槛已通过；生产客户端已生成 `deploy/windows-production/BTC Paper Trading Setup.exe`；最终 release 已生成并上传到远程。  
遗留风险：HTTP 是临时明文方案。  
下一步：已完成远程部署。

## Phase 3 上传与部署

完成时间：2026-05-08 20:32  
执行命令：见 `docs/release-and-deploy-workflow.md`。  
结果：release 已上传到 `/srv/p-t/releases/P_T_0.1.0_20260508_203203` 并同步到 `/srv/p-t/app`；checksum 校验通过。  
遗留风险：服务器磁盘空间需持续关注。  
下一步：已执行数据库迁移。

## Phase 4 数据库迁移与 Admin 创建
admin的账号：superadmin 密码：HTT2026@
完成时间：2026-05-08 20:50  
执行命令：
```bash
APP_ENV_FILE=.env.production docker compose -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npm run db:backup
APP_ENV_FILE=.env.production docker compose -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npm run db:migrate
APP_ENV_FILE=.env.production docker compose -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npm run db:status
APP_ENV_FILE=.env.production docker compose -f docker-compose.deploy.yml --env-file .env.production run --rm app-server npx tsx scripts/create-admin.ts
```

结果：`000000` 到 `000004` 全部 applied；Admin 已创建；服务栈已启动。  
遗留风险：Admin 初始密码保存在远程 root-only 文件 `/srv/p-t/deployment-secrets-current.txt`，首次登录后应立即修改。  
下一步：已完成外部验收。

## Phase 5 外部访问与客户端 smoke

完成时间：2026-05-08 20:52  
执行命令：

```bash
curl -f http://103.147.13.98:10001/api/health/live
curl -f http://103.147.13.98:10001/api/health/ready
```

结果：外部 `10001/tcp` 连通；`/api/health/live` 和 `/api/health/ready` 均返回 ok；Admin 登录 API smoke 通过。  
遗留风险：30/50 客户端压力测试已在 Phase 7 补做并通过。  
下一步：已完成文档和回滚确认。

## Phase 6 文档和回滚确认

完成时间：2026-05-08 20:55  
执行命令：核对 `生产环境配置说明.md` 和 `打包发布工作流.md`。  
结果：已新增中文生产环境配置和打包发布工作流文档，并同步到远程部署目录。  
遗留风险：HTTP 是临时方案，后续必须升级 HTTPS。  
下一步：进入后续快速迭代流程。

## Phase 7 客户端连接压力测试与稳定性修复

完成时间：2026-05-09 00:16  
执行命令：

```bash
npm run test:production-client-load
```

关键参数：

```text
LOAD_TEST_BASE_URL=http://103.147.13.98:10001
LOAD_TEST_PREFIX=load_20260508_234150
LOAD_TEST_ONLINE_CLIENTS=30
LOAD_TEST_ONLINE_MINUTES=30
LOAD_TEST_RECONNECT_CLIENTS=50
LOAD_TEST_RECONNECT_ROUNDS=5
```

修复内容：

```text
pg idle client error 不再关闭整个 PostgreSQL pool。
WS heartbeat 调整为连续 3 次未响应才断开。
生产参数调整：PG_MAX_CONNECTIONS=30、PG_CONNECTION_TIMEOUT_MS=30000、PG_IDLE_TIMEOUT_MS=60000、MARKET_WS_MIN_INTERVAL_MS=1000、GAMMA_POLL_INTERVAL_MS=3000。
```

结果：30 个客户端在线 30 分钟通过，最终 30/30 在线、60/60 WS open、异常断开 0、health 全程 ok；50 个客户端 5 轮重连通过，最终 50/50 在线、100/100 WS open、异常断开 0、health 全程 ok。  
遗留风险：当前仍是 HTTP 临时部署；本次是协议级模拟客户端测试，不是真实 Electron UI 同屏测试；服务器曾无法拉 Docker Hub，后续标准镜像重建需确认镜像源。  
下一步：按 `docs/打包发布工作流.md` 进入后续迭代发布。

## Phase 8 镜像备份与构建通道确认

完成时间：2026-05-09 08:31  
执行命令：

```bash
docker pull node:24-alpine
docker save app-app-server:latest app-matching-service:latest caddy:2-alpine postgres:17-alpine redis:8-alpine -o /srv/p-t/releases/image-backups/p-t-fixed-images_<timestamp>.tar
gzip /srv/p-t/releases/image-backups/p-t-fixed-images_<timestamp>.tar
sha256sum /srv/p-t/releases/image-backups/p-t-fixed-images_<timestamp>.tar.gz
```

结果：远程标准构建仍不可用，`registry-1.docker.io` 返回 `unifi.local/localhost` 证书；已保存当前可用生产镜像离线包 `/srv/p-t/releases/image-backups/p-t-fixed-images_20260509_003005.tar.gz`，并生成 sha256 校验文件。  
遗留风险：远程 Docker Hub/DNS/代理问题未修复前，不应依赖生产机实时拉取基础镜像。  
下一步：后续迭代优先使用已验证 release；如需标准镜像重建，先修复镜像源或改成本地构建后 `docker save/load` 上传。

## Phase 9 Windows 客户端代理兼容修复

完成时间：2026-05-09 08:46  
执行命令：
```bash
curl.exe --noproxy "*" http://103.147.13.98:10001/api/health/live
curl.exe http://103.147.13.98:10001/api/health/live
npm run test:electron-config
ALLOW_INSECURE_PROD_HTTP=true VITE_API_BASE_URL=http://103.147.13.98:10001 npm run package:win:prod
```

结果：确认服务端和公网 `10001` 正常；本机系统代理 `127.0.0.1:7897` 会导致客户端健康检查超时并显示“后端离线”。已给当前 Windows 代理绕过列表加入 `103.147.13.98` 和 `103.147.13.98:10001`，并修改生产客户端：打包时写入 `productionApiBaseUrl`，Electron 启动时自动配置 `proxy-bypass-list`。  
遗留风险：当前已安装的旧客户端不包含内置代理绕过逻辑；需要使用重新生成的安装包覆盖安装，或保持 Windows 代理绕过列表包含生产 IP。  
下一步：分发修复后的 `deploy/windows-production/BTC Paper Trading Setup.exe`。
